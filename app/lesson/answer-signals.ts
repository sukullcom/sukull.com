/**
 * `selectedOption` sentinels for {@link app/lesson/quiz.tsx} when the
 * "Kontrol et" path does not map to a single `challenge_options` row.
 */
export const ANSWER_WRONG = -1;
export const ANSWER_CORRECT = -2;
/** Sıralama / eşleştirme: cevap hazır, buton açık; doğruluk ayrıca hesaplanır. */
export const READY_TO_CHECK = -3;
