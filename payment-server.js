const express = require('express');
const cors = require('cors');
const Iyzipay = require('iyzipay');
const { createClient } = require('@supabase/supabase-js');
const { drizzle } = require('drizzle-orm/node-postgres');
const { Pool } = require('pg');
const { eq } = require('drizzle-orm');
// Load environment variablesrequire('dotenv').config();
const app = express();
const PORT = process.env.PORT || process.env.PAYMENT_SERVER_PORT || 3001;
console.log('🚀 Starting payment server...');
console.log('📊 Environment:', process.env.NODE_ENV || 'development');
console.log('🔌 Port:', PORT);
console.log('🌐 Database URL:', process.env.DATABASE_URL ? 'Set' : 'Not set');
console.log('🔑 Supabase URL:', process.env.NEXT_PUBLIC_SUPABASE_URL ? 'Set' : 'Not set');
// Define schema inline since CommonJS import from TypeScript is complexconst userCreditsSchema = {  id: 'serial',  userId: 'text',  totalCredits: 'integer',  usedCredits: 'integer',   availableCredits: 'integer',  createdAt: 'timestamp',  updatedAt: 'timestamp'};
const creditTransactionsSchema = {  id: 'serial',  userId: 'text',  paymentId: 'text',  creditsAmount: 'integer',  totalPrice: 'text',  currency: 'text',  status: 'text',  createdAt: 'timestamp'};
const paymentLogsSchema = {  id: 'serial',  userId: 'text',  paymentId: 'text',  requestData: 'jsonb',  responseData: 'jsonb',  status: 'text',  errorCode: 'text',  errorMessage: 'text',  createdAt: 'timestamp'};
// Middlewareapp.use(cors({  origin: function (origin, callback) {    // Allow requests with no origin (like mobile apps or curl requests)    if (!origin) return callback(null, true);        const allowedOrigins = [      'https://sukull.com',      'https://www.sukull.com',      'http://localhost:3000',      process.env.NEXT_PUBLIC_APP_URL    ].filter(Boolean);        console.log('CORS check - Origin:', origin, 'Allowed:', allowedOrigins);        if (allowedOrigins.includes(origin)) {      return callback(null, true);    } else {      console.log('CORS blocked origin:', origin);      return callback(new Error('Not allowed by CORS'));    }  },  credentials: true,  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],  allowedHeaders: ['Content-Type', 'Authorization', 'X-Requested-With']}));
app.use(express.json());
// Initialize Supabase client for authenticationlet supabase;
try {  if (process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.SUPABASE_SERVICE_ROLE_KEY) {    supabase = createClient(  process.env.NEXT_PUBLIC_SUPABASE_URL,  process.env.SUPABASE_SERVICE_ROLE_KEY);    console.log('✅ Supabase client initialized');  } else {    console.log('⚠️ Supabase credentials missing');  }} catch (error) {  console.error('❌ Supabase initialization error:', error.message);}
// Initialize database connectionlet pool;
try {  if (process.env.DATABASE_URL) {    pool = new Pool({  connectionString: process.env.DATABASE_URL,  ssl: process.env.NODE_ENV === 'production'     ? {        rejectUnauthorized: false,        ca: process.env.CA_CERT      }    : false});    console.log('✅ Database pool initialized');        // Test database connection    pool.connect()      .then(client => {        console.log('✅ Database connection test successful');        client.release();      })      .catch(err => {        console.error('⚠️ Database connection test failed:', err.message);      });  } else {    console.log('⚠️ DATABASE_URL not set - database features disabled');  }} catch (error) {  console.error('❌ Database initialization error:', error.message);}
// Initialize Iyzicolet iyzipay;
try {  if (!process.env.IYZICO_API_KEY || !process.env.IYZICO_SECRET_KEY) {    console.error('⚠️ IYZICO_API_KEY and IYZICO_SECRET_KEY not set - payments will not work');  }  iyzipay = new Iyzipay({    apiKey: process.env.IYZICO_API_KEY || '',    secretKey: process.env.IYZICO_SECRET_KEY || '',    uri: process.env.IYZICO_BASE_URL || 'https://sandbox-api.iyzipay.com',  });  console.log('✅ Iyzico initialized');} catch (error) {  console.error('❌ Iyzico initialization error:', error.message);}
// Authentication middlewareconst authenticateUser = async (req, res, next) => {  try {    if (!supabase) {      return res.status(500).json({         success: false,         message: 'Authentication service not available'       });    }    const authHeader = req.headers.authorization;    if (!authHeader || !authHeader.startsWith('Bearer ')) {      return res.status(401).json({         success: false,         message: 'Authorization header required'       });    }    const token = authHeader.substring(7);        // Verify the JWT token with Supabase    const { data: { user }, error } = await supabase.auth.getUser(token);        if (error || !user) {      return res.status(401).json({         success: false,         message: 'Invalid or expired token'       });    }    req.user = user;    next();  } catch (error) {    console.error('Authentication error:', error);    res.status(401).json({       success: false,       message: 'Authentication failed'     });  }};
// Health check endpointapp.get('/health', (req, res) => {  const health = {    success: true,     message: 'Payment server is running',    timestamp: new Date().toISOString(),    services: {      supabase: !!supabase,      database: !!pool,      iyzico: !!iyzipay    },    environment: process.env.NODE_ENV || 'development',    port: PORT  };    console.log('Health check requested:', health);  res.json(health);});
// Basic ping endpoint for Railwayapp.get('/ping', (req, res) => {  res.json({ status: 'ok', timestamp: new Date().toISOString() });});
// Root endpointapp.get('/', (req, res) => {  res.json({     message: 'Sukull Payment Server',    status: 'running',    endpoints: {      health: '/health',      ping: '/ping',      payment: '/api/payment/create'    },    timestamp: new Date().toISOString()  });});
// Handle preflight requests for payment endpointsconst handlePreflight = (req, res) => {  res.header('Access-Control-Allow-Origin', req.headers.origin);  res.header('Access-Control-Allow-Methods', 'POST, OPTIONS');  res.header('Access-Control-Allow-Headers', 'Content-Type, Authorization, X-Requested-With');  res.header('Access-Control-Allow-Credentials', 'true');  res.sendStatus(200);};
app.options('/api/payment/create', handlePreflight);
app.options('/api/payment/subscribe', handlePreflight);
// Create payment endpointapp.post('/api/payment/create', authenticateUser, async (req, res) => {  try {    if (!pool || !iyzipay) {      return res.status(500).json({        success: false,        message: 'Payment service not available'      });    }    const user = req.user;    const { creditsAmount, totalPrice, paymentCard, billingAddress } = req.body;    // Validate required fields    if (!creditsAmount || !totalPrice || !paymentCard || !billingAddress) {      return res.status(400).json({         success: false,         message: "Eksik ödeme bilgileri"       });    }    // Generate unique IDs    const conversationId = `conv_${user.id}_${Date.now()}`;    const basketId = `basket_${user.id}_${Date.now()}`;    // Prepare Iyzico payment request    const request_data = {      locale: "tr",      conversationId: conversationId,      price: totalPrice.toString(),      paidPrice: totalPrice.toString(),      currency: Iyzipay.CURRENCY.TRY,      installment: '1',      basketId: basketId,      paymentChannel: Iyzipay.PAYMENT_CHANNEL.WEB,      paymentGroup: Iyzipay.PAYMENT_GROUP.PRODUCT,      paymentCard: {        cardHolderName: paymentCard.cardHolderName,        cardNumber: paymentCard.cardNumber,        expireMonth: paymentCard.expireMonth,        expireYear: paymentCard.expireYear,        cvc: paymentCard.cvc,        registerCard: '0'      },      buyer: {        id: user.id,        name: billingAddress.contactName.split(' ')[0] || 'Unknown',        surname: billingAddress.contactName.split(' ').slice(1).join(' ') || 'User',        gsmNumber: billingAddress.phone || '+905350000000',        email: user.email || 'user@example.com',        identityNumber: '74300864791', // Test identity number        lastLoginDate: new Date().toISOString().slice(0, 19).replace('T', ' '),        registrationDate: new Date().toISOString().slice(0, 19).replace('T', ' '),        registrationAddress: billingAddress.address,        ip: req.ip || '85.34.78.112', // Use client IP or default for sandbox        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        zipCode: billingAddress.zipCode || '34000'      },      shippingAddress: {        contactName: billingAddress.contactName,        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        address: billingAddress.address,        zipCode: billingAddress.zipCode || '34000'      },      billingAddress: {        contactName: billingAddress.contactName,        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        address: billingAddress.address,        zipCode: billingAddress.zipCode || '34000'      },      basketItems: [        {          id: `credit_${creditsAmount}`,          name: `${creditsAmount} Ders Kredisi`,          category1: 'Education',          category2: 'Credits',          itemType: 'VIRTUAL',          price: totalPrice.toString()        }      ]    };    // Process payment with Iyzico    const paymentResult = await new Promise((resolve, reject) => {      iyzipay.payment.create(request_data, (err, result) => {        if (err) {          console.error('Iyzico payment error:', err);          reject(err);        } else {          console.log('Iyzico payment result:', result);          resolve(result);        }      });    });    const client = await pool.connect();        try {      await client.query('BEGIN');      // Log the payment attempt      await client.query(        `INSERT INTO payment_logs (user_id, payment_id, request_data, response_data, status, error_code, error_message, created_at)          VALUES ($1, $2, $3, $4, $5, $6, $7, NOW())`,        [          user.id,          paymentResult.paymentId || conversationId,          JSON.stringify(request_data),          JSON.stringify(paymentResult),          paymentResult.status || 'failed',          paymentResult.errorCode || null,          paymentResult.errorMessage || null        ]      );      if (paymentResult.status === 'success') {        await client.query(          `INSERT INTO credit_transactions (user_id, payment_id, credits_amount, total_price, currency, status, created_at)            VALUES ($1, $2, $3, $4, $5, $6, NOW())`,          [user.id, paymentResult.paymentId, creditsAmount, totalPrice.toString(), 'TRY', 'success']        );        const existingCredits = await client.query(          'SELECT * FROM user_credits WHERE user_id = $1 FOR UPDATE',          [user.id]        );                if (existingCredits.rows.length > 0) {          const current = existingCredits.rows[0];          await client.query(            `UPDATE user_credits              SET total_credits = $1, available_credits = $2, updated_at = NOW()              WHERE user_id = $3`,            [              current.total_credits + creditsAmount,              current.available_credits + creditsAmount,              user.id            ]          );        } else {          await client.query(            `INSERT INTO user_credits (user_id, total_credits, used_credits, available_credits, created_at, updated_at)              VALUES ($1, $2, 0, $3, NOW(), NOW())`,            [user.id, creditsAmount, creditsAmount]          );        }        await client.query('COMMIT');        res.json({          success: true,          message: `Ödeme başarılı! ${creditsAmount} kredi hesabınıza eklendi.`,          data: {            paymentId: paymentResult.paymentId,            creditsAdded: creditsAmount,          }        });      } else {        await client.query(          `INSERT INTO credit_transactions (user_id, payment_id, credits_amount, total_price, currency, status, created_at)            VALUES ($1, $2, $3, $4, $5, $6, NOW())`,          [user.id, paymentResult.paymentId || conversationId, creditsAmount, totalPrice.toString(), 'TRY', 'failed']        );        await client.query('COMMIT');        res.status(400).json({          success: false,          message: paymentResult.errorMessage || 'Ödeme başarısız',        });      }    } catch (dbError) {      await client.query('ROLLBACK');      throw dbError;    } finally {      client.release();    }  } catch (error) {    console.error('Payment creation error:', error);        res.status(500).json({      success: false,      message: 'Ödeme işlemi sırasında hata oluştu',    });  }});
// Subscription payment endpointapp.post('/api/payment/subscribe', authenticateUser, async (req, res) => {  try {    if (!pool || !iyzipay) {      return res.status(500).json({        success: false,        message: 'Payment service not available'      });    }    const { paymentCard, billingAddress } = req.body;    const user = req.user;    if (!paymentCard || !billingAddress) {      return res.status(400).json({        success: false,        message: 'Eksik ödeme bilgileri',      });    }    // Subscription details    const subscriptionAmount = 100; // 100 TL per month    const conversationId = `sub_${Date.now()}_${user.id}`;    const basketId = `basket_${conversationId}`;    const request_data = {      locale: Iyzipay.LOCALE.TR,      conversationId: conversationId,      price: subscriptionAmount.toString(),      paidPrice: subscriptionAmount.toString(),      currency: Iyzipay.CURRENCY.TRY,      installment: '1',      basketId: basketId,      paymentChannel: Iyzipay.PAYMENT_CHANNEL.WEB,      paymentGroup: Iyzipay.PAYMENT_GROUP.SUBSCRIPTION,      paymentCard: {        cardHolderName: paymentCard.cardHolderName,        cardNumber: paymentCard.cardNumber,        expireMonth: paymentCard.expireMonth,        expireYear: paymentCard.expireYear,        cvc: paymentCard.cvc,        registerCard: '0'      },      buyer: {        id: user.id,        name: billingAddress.contactName.split(' ')[0] || 'Unknown',        surname: billingAddress.contactName.split(' ').slice(1).join(' ') || 'User',        gsmNumber: billingAddress.phone || '+905350000000',        email: user.email || 'user@example.com',        identityNumber: '74300864791', // Test identity number        lastLoginDate: new Date().toISOString().slice(0, 19).replace('T', ' '),        registrationDate: new Date().toISOString().slice(0, 19).replace('T', ' '),        registrationAddress: billingAddress.address,        ip: req.ip || '85.34.78.112',        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        zipCode: billingAddress.zipCode || '34000'      },      shippingAddress: {        contactName: billingAddress.contactName,        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        address: billingAddress.address,        zipCode: billingAddress.zipCode || '34000'      },      billingAddress: {        contactName: billingAddress.contactName,        city: billingAddress.city,        country: billingAddress.country || 'Turkey',        address: billingAddress.address,        zipCode: billingAddress.zipCode || '34000'      },      basketItems: [        {          id: 'infinite_hearts_subscription',          name: 'Sonsuz Can - Aylık Abonelik',          category1: 'Subscription',          category2: 'Premium',          itemType: 'VIRTUAL',          price: subscriptionAmount.toString()        }      ]    };    // Process payment with Iyzico    const paymentResult = await new Promise((resolve, reject) => {      iyzipay.payment.create(request_data, (err, result) => {        if (err) {          console.error('Iyzico subscription payment error:', err);          reject(err);        } else {          console.log('Iyzico subscription payment result:', result);          resolve(result);        }      });    });    const client = await pool.connect();        try {      await client.query('BEGIN');      await client.query(        `INSERT INTO payment_logs (user_id, payment_id, request_data, response_data, status, error_code, error_message, created_at)          VALUES ($1, $2, $3, $4, $5, $6, $7, NOW())`,        [          user.id,          paymentResult.paymentId || conversationId,          JSON.stringify(request_data),          JSON.stringify(paymentResult),          paymentResult.status || 'failed',          paymentResult.errorCode || null,          paymentResult.errorMessage || null        ]      );      if (paymentResult.status === 'success') {        const now = new Date();        const endDate = new Date(now);        endDate.setMonth(endDate.getMonth() + 1);        await client.query(          `INSERT INTO user_subscriptions (user_id, subscription_type, status, start_date, end_date, payment_id, amount, currency, created_at, updated_at)            VALUES ($1, $2, $3, $4, $5, $6, $7, $8, NOW(), NOW())`,          [user.id, 'infinite_hearts', 'active', now, endDate, paymentResult.paymentId, subscriptionAmount.toString(), 'TRY']        );        await client.query(          `UPDATE user_progress            SET has_infinite_hearts = true, subscription_expires_at = $1            WHERE user_id = $2`,          [endDate, user.id]        );        await client.query('COMMIT');        res.json({          success: true,          message: 'Sonsuz can aboneliği başarıyla aktifleştirildi! Artık sınırsız kalp kullanabilirsiniz.',          data: {            paymentId: paymentResult.paymentId,            subscriptionType: 'infinite_hearts',            expiresAt: endDate.toISOString(),          }        });      } else {        await client.query('COMMIT');        res.status(400).json({          success: false,          message: paymentResult.errorMessage || 'Abonelik ödemesi başarısız',        });      }    } catch (dbError) {      await client.query('ROLLBACK');      throw dbError;    } finally {      client.release();    }  } catch (error) {    console.error('Subscription payment error:', error);        res.status(500).json({      success: false,      message: 'Abonelik işlemi sırasında hata oluştu',    });  }});
// Error handling middlewareapp.use((error, req, res, next) => {  console.error('Server error:', error);  res.status(500).json({    success: false,    message: 'Internal server error',    error: process.env.NODE_ENV === 'development' ? error.message : 'Something went wrong'  });});
// Handle uncaught exceptionsprocess.on('uncaughtException', (error) => {  console.error('Uncaught Exception:', error);  // Don't exit in production, just log the error  if (process.env.NODE_ENV !== 'production') {    process.exit(1);  }});
// Handle unhandled promise rejectionsprocess.on('unhandledRejection', (reason, promise) => {  console.error('Unhandled Rejection at:', promise, 'reason:', reason);  // Don't exit in production, just log the error  if (process.env.NODE_ENV !== 'production') {    process.exit(1);  }});
// Start serverapp.listen(PORT, '0.0.0.0', () => {  console.log(`🚀 Payment server running on port ${PORT}`);  console.log(`🏥 Health check: http://0.0.0.0:${PORT}/health`);  console.log(`💳 Payment endpoint: http://0.0.0.0:${PORT}/api/payment/create`);  console.log(`♾️  Subscription endpoint: http://0.0.0.0:${PORT}/api/payment/subscribe`);  console.log(`🌍 Environment: ${
process.env.NODE_ENV || 'development'}`);  console.log(`🔒 Iyzico: ${
process.env.IYZICO_BASE_URL || 'sandbox'}`);  console.log(`📊 Services status:`);  console.log(`   - Supabase: ${supabase ? '✅' : '❌'}`);  console.log(`   - Database: ${pool ? '✅' : '❌'}`);  console.log(`   - Iyzico: ${iyzipay ? '✅' : '❌'}`);}).on('error', (err) => {  console.error('❌ Server startup error:', err);  process.exit(1);});
